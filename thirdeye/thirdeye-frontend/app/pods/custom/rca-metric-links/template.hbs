{{#if record.links.length}}
  {{#each record.links as |link|}}
    {{#each-in link as |linkLabel url|}}
      <a href="{{url}}" target="_blank">{{linkLabel}}</a>
    {{/each-in}}
  {{/each}}
{{else}}
  -
{{/if}}
