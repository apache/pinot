{{!-- Custom Cell Template for Ember-models-table
      There is another checkbox template in /custom/checkbox that is being used for the current rca page.
      Since that page will soon be deprecated, so will that template. This is meant to replace it--}}
{{input
  type="checkbox"
  checked=record.isSelected}}