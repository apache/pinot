<button {{action "setMode" "change"}}>change</button> |
<button {{action "setMode" "contributionDiff"}}>contribution diff</button> |
<button {{action "setMode" "contributionToDiff"}}>contribution to diff</button>

<button {{action "setRollup" "5"}}>top 5</button> |
<button {{action "setRollup" "10"}}>top 10</button> |
<button {{action "setRollup" "20"}}>top 20</button>

<ol>
  {{#each-in scores as |dimName dimValues|}}
    <li>{{dimName}}
        {{#each-in dimValues as |dimValue value|}}
          | {{dimValue}} ({{value}})
        {{else}}
          | No values
        {{/each-in}}
    </li>
  {{else}}
    <li>No dimensions</li>
  {{/each-in}}
</ol>
