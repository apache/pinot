<div class="filter-bar">
  {{#each filterBlocks as |block|}}
    <section class="filter-bar__group">
      <h5 class="filter-bar__group-title">
        {{block.header}}
        <a class="filter-bar__group-toggle" {{action 'selectEventType' block.eventType}}>
          <i class="glyphicon {{if block.isHidden " glyphicon-menu-up " "glyphicon-menu-down "}}"></i>
        </a>
      </h5>
      <ul class="filter-bar__group-list {{if block.isHidden " filter-bar__group-list--hidden "}}">
        {{#each block.inputs as |filter|}}
          {{filter-bar-input
            eventType=block.eventType
            labelMapping=filter.labelMapping
            label=filter.label
            type=filter.type
            attributesMap=attributesMap
            onFilterChange=(action "onFilterChange")}}
        {{/each}}
      </ul>
    </section>
  {{/each}}
</div>