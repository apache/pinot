import EventsTable from 'thirdeye-frontend/pods/components/events-table/component';

export default EventsTable.extend({
  /**
   * @type {String}
   * overrides EventsTable's default value for selected tab
   */
  selectedTab: 'holiday'
});
