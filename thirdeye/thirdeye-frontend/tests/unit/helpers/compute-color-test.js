import { computeColor } from 'thirdeye-frontend/helpers/compute-color';
import { module, test } from 'qunit';

module('Unit | Helper | compute color', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = computeColor([42]);
    assert.ok(result);
  });
});
