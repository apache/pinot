import { computeTextColor } from 'thirdeye-frontend/helpers/compute-text-color';
import { module, test } from 'qunit';

module('Unit | Helper | compute text color', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = computeTextColor([42]);
    assert.ok(result);
  });
});
