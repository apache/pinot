<?xml version="1.0"?>
<!--

    Licensed to the Apache Software Foundation (ASF) under one
    or more contributor license agreements.  See the NOTICE file
    distributed with this work for additional information
    regarding copyright ownership.  The ASF licenses this file
    to you under the Apache License, Version 2.0 (the
    "License"); you may not use this file except in compliance
    with the License.  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing,
    software distributed under the License is distributed on an
    "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
    KIND, either express or implied.  See the License for the
    specific language governing permissions and limitations
    under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>pinot-input-format</artifactId>
    <groupId>org.apache.pinot</groupId>
    <version>1.6.0-SNAPSHOT</version>
  </parent>

  <artifactId>pinot-parquet</artifactId>
  <name>Pinot Parquet</name>
  <url>https://pinot.apache.org/</url>
  <properties>
    <pinot.root>${basedir}/../../..</pinot.root>
    <shade.phase.prop>package</shade.phase.prop>
  </properties>
  <dependencies>
    <dependency>
      <groupId>org.apache.pinot</groupId>
      <artifactId>pinot-avro-base</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.parquet</groupId>
      <artifactId>parquet-avro</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-common</artifactId>
      <scope>${hadoop.dependencies.scope}</scope>
      <!-- org.eclipse.jetty (CVE-2026-2332) is excluded centrally on the managed hadoop-common
           dependency in the root pom. The hadoop-client-runtime uber-jar below bundles its own
           relocated Jetty, which a Maven exclusion cannot reach; that is stripped by the
           shade-plugin filter further down. -->
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-client-runtime</artifactId>
      <scope>${hadoop.dependencies.scope}</scope>
    </dependency>
    <dependency>
      <groupId>org.codehaus.woodstox</groupId>
      <artifactId>stax2-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop.thirdparty</groupId>
      <artifactId>hadoop-shaded-protobuf_3_25</artifactId>
    </dependency>

    <!-- Used for Parquet Writer in tests. Kept at test scope so YARN/Jetty
         transitives (hadoop-yarn-client -> websocket-client -> jetty-client ->
         jetty-http) do not leak into runtime distributions. -->
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-mapreduce-client-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.pinot</groupId>
      <artifactId>pinot-segment-local</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <configuration>
          <!-- combine.children=append keeps the inherited root-pom filters (signed manifests,
               module-info, etc.) and adds the hadoop-client-runtime filter below. -->
          <filters combine.children="append">
            <filter>
              <artifact>org.apache.hadoop:hadoop-client-runtime</artifact>
              <excludes>
                <!-- hadoop-client-runtime is an uber-jar that bundles Hadoop's own relocated
                     Jetty (org.apache.hadoop.shaded.org.eclipse.jetty). It exists only for
                     Hadoop's embedded HttpServer2 web UIs, which Parquet reading never starts,
                     so the classes are unreachable here. The leftover jetty-* Maven metadata
                     still makes scanners flag CVE-2026-2332 (jetty-http request smuggling).
                     Strip both the relocated classes and the metadata from the shaded jar. -->
                <exclude>org/apache/hadoop/shaded/org/eclipse/jetty/**</exclude>
                <exclude>META-INF/maven/org.eclipse.jetty/**</exclude>
                <exclude>META-INF/maven/org.eclipse.jetty.websocket/**</exclude>
                <!-- ServiceLoader entries whose implementation classes are removed above. -->
                <exclude>META-INF/services/org.apache.hadoop.shaded.org.eclipse.jetty.**</exclude>
              </excludes>
            </filter>
          </filters>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>pinot-fastdev</id>
      <properties>
        <shade.phase.prop>none</shade.phase.prop>
      </properties>
    </profile>
  </profiles>
</project>
