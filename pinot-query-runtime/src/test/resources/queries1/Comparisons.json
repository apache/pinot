{
  "numerics_comp": {
    "psql": "9.2",
    "tables": {
      "tbl1": {
        "schema": [
          {"name": "i1", "type": "BIG_DECIMAL"},
          {"name": "i2", "type": "BIG_DECIMAL"}
        ],
        "inputs": [
          ["1.1", "1.2"], ["1.1", "1.2"]
        ]
      },
      "tbl2": {
        "schema": [
          {"name": "i1", "type": "BIG_DECIMAL"},
          {"name": "i3", "type": "BIG_DECIMAL"}
        ],
        "inputs": [
          ["1.1", "1.4"], ["1.1", "1.3"]
        ]
      }
    },
    "queries": [
      {
        "ignored": false,
        "comment": "fails with IllegalStateException... not sure why!",
        "sql": "SELECT * FROM {tbl1} JOIN {tbl2} ON {tbl1}.i1 = {tbl2}.i1 WHERE {tbl1}.i2 >= {tbl2}.i3"
      }
    ]
  }
}
